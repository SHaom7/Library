@using Library.Models
@{
    ViewData["Title"] = "Books";
}
@model IEnumerable<Book>;

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-4">
            <div class="card mb-4 box-shadow">
                <div class="card-body">
                    <p class="card-text">@item.Title</p>
                    <div class="d-flex justify-content-between align-items-center">
                        <div class="btn-group">
                            <a asp-controller="Book" asp-action="Detail" asp-route-itemid="@item.BookId" type="btn btn-success" class="btn btn-sm btn-outline-secondary">View</a>
                            <a asp-controller="Book" asp-action="Detail" asp-route-itemid="@item.BookId" type="btn btn-success" class="btn btn-sm btn-outline-secondary">Edit</a>
                        </div>
                        <small class="text-muted">9 mins</small>
                    </div>
                </div>
            </div>
        </div>
    }
</div>